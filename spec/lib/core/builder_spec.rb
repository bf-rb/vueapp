# frozen_string_literal: true

RSpec.describe VueApp::Core::Builder do
  it '#initialize' do
    # builder = VueApp::Core::Builder.new
    # expect(builder.env_is_a(:development)).to eql(true)
  end
end
